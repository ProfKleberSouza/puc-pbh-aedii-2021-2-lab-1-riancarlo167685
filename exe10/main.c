#include <stdio.h>
#include "functions.h"

int main() {

    int n;
  
  // ENTRADA DE DADOS
  scanf("%d", &n);

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  tabela_verdade(n);

  return(0);
}