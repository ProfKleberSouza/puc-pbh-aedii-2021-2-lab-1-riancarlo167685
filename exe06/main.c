#include <stdio.h>
#include "functions.h"

int main() {

    int a, b;
  
  // ENTRADA DE DADOS
  scanf("%d %d", &a, &b);

  // SAIDA - IMPRIMINDO O RESULTADO
  printf("%d", potencia( a, b));

  return(0);
}